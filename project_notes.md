# 📝 Python Script Notes

## 🇹🇭 Bangkok What-If

- `matches = 11` → Number of matches.
- `stadiums = 3` → My idea uses 3 stadiums.
- `matches / stadiums` → Finds the average matches per stadium.
- `round(..., 2)` → Rounds the answer to 2 decimal places.
- Result = **3.67 matches per stadium**.

---

## 🇸🇦 Jeddah Reality

- `matches = 11` → Number of matches.
- `stadiums = 2` → Jeddah used 2 stadiums.
- `matches / stadiums` → Finds the average matches per stadium.
- `round(..., 2)` → Rounds the answer to 2 decimal places.
- Result = **5.5 matches per stadium**.

---

## 💡 What the Scripts Show

**Bangkok what-if:** 3.67 matches per stadium  
**Jeddah reality:** 5.5 matches per stadium

The simple idea is:

> **Same number of matches + more stadiums = fewer matches per stadium on average.**

The Python is not predicting what would happen.

It is simply calculating and comparing the two setups.
